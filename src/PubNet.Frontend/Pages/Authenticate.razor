@page "/authenticate"
@inject IAuthenticationService Auth
@inject OnboardingService Onboarding
@inject NavigationManager Nav

@if (MessageBoxContent is not null)
{
	<Alert Color="Color.Warning" Dismisable Visible>
		@MessageBoxContent
	</Alert>
}

<Heading Size="HeadingSize.Is1">Login</Heading>

<LoginForm/>

<Divider/>

@if (Error is not null)
{
	<ExceptionBox Exception="Error"/>
}
else
{
	switch (ShowRegistration)
	{
		case true:
			<Heading Size="HeadingSize.Is1">Register</Heading>

			<RegisterForm Model="_registration" OnValidSubmit="RegisterAsync" Submitting="_registering"
			              Error="@_registerError" Success="@_registerSuccess"/>
			break;
		case null:
			<p>
				Checking if registrations are enabled...
			</p>
			break;
		case false:
			<p>
				Registrations are currently disabled.
			</p>
			break;
	}
}

@code {

	private bool? ShowRegistration { get; set; }

	private Exception? Error { get; set; }

	private bool _registering;

	private RegisterRequestDto _registration = new();

	private string? _registerError;

	private string? _registerSuccess;

	[Parameter]
	[SupplyParameterFromQuery(Name = "message")]
	public string? Message { get; set; }

	private string? MessageBoxContent => Message switch
	{
		"session-expired" => "Your session has expired. Please log in again.",
		_ => null,
	};

	protected override async Task OnInitializedAsync()
	{
		await base.OnInitializedAsync();

		Error = null;
		ShowRegistration = null;

		// there is nothing to log into yet, and the layout only checks this when it is first created, so a
		// client-side navigation from anywhere else would land here
		if (await Onboarding.IsPendingAsync())
		{
			Nav.NavigateTo("/onboarding");

			return;
		}

		try
		{
			ShowRegistration = await Auth.GetRegistrationsEnabledAsync();
		}
		catch (Exception e)
		{
			Error = e;
		}
	}

	private async Task RegisterAsync()
	{
		_registerError = null;
		_registerSuccess = null;
		_registering = true;

		try
		{
			_ = await Auth.RegisterAsync(_registration.Email!, _registration.Name!, _registration.Password!,
				_registration.Username!, _registration.Website);

			_registration = new();

			_registerSuccess = "Registration complete. You can now log in.";
		}
		catch (PubNetSdkException e)
		{
			_registerError = e.Message;
		}
		finally
		{
			_registering = false;
		}
	}


}
